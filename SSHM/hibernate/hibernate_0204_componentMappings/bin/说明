组件的配置

首先了解一下什么是组件。
组件是一个被包含的对象，
它和它的所有者同存于一张表中，
也就是说它仅仅是个值类型，而不是一个实体。
值类型和实体的区别在于值类型没有标识符，
当然了持久化一个值类型也就不需要标识符属性

采用component映射的好处：
它实现了对象模型的---细粒度---划分，层次会更分明，复用率会更高


报错：
 IllegalArgumentException（非法理由）
               
       


